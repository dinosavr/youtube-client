import { SearchWordFilterPipe } from './search-word-filter.pipe';

describe('SearchWordFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWordFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
