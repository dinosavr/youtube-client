import { SortSearchResultPipe } from './sort-search-result.pipe';

describe('SortSearchResultPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSearchResultPipe();
    expect(pipe).toBeTruthy();
  });
});
