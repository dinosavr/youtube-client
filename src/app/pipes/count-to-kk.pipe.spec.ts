import { CountToKKPipe } from './count-to-kk.pipe';

describe('CountToKKPipe', () => {
  it('create an instance', () => {
    const pipe = new CountToKKPipe();
    expect(pipe).toBeTruthy();
  });
});
