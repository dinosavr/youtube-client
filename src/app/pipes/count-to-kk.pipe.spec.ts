import { CountToKKPipe } from './count-to-kk.pipe';

describe('CountToKKPipe', () => {
  it('create an instance', () => {
    const pipe: CountToKKPipe = new CountToKKPipe();
    expect(pipe).toBeTruthy();
  });
});
